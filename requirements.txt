# pip install -r requirements.txt
# Core dependencies (used by all scripts)
pandas>=3.0
numpy>=2.4
openpyxl>=3.1
xlsxwriter>=3.2

# QR code tools (only needed for qr-code/ scripts)
qrcode>=7.4
pyzbar>=0.1.9
Pillow>=12.0
requests>=2.31
